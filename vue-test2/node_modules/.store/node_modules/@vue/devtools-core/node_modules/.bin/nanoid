#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -z "$NODE_PATH" ]; then
  export NODE_PATH="$basedir/E:/School/24-25-1/test/vue-test2/node_modules/.store/nanoid@5.0.9/node_modules"
else
  export NODE_PATH="$NODE_PATH:$basedir/E:/School/24-25-1/test/vue-test2/node_modules/.store/nanoid@5.0.9/node_modules"
fi
if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../../../nanoid@5.0.9/node_modules/nanoid/bin/nanoid.js" "$@"
else
  exec node  "$basedir/../../../../../../nanoid@5.0.9/node_modules/nanoid/bin/nanoid.js" "$@"
fi
